# BloodHound Fix Computers Script

Quickfix script to allow BloodHound legacy to import latest SharpHound data.

Install:

```bash
pipx install git+https://github.com/emanuelduss/bloodhound-fix-computers
```
